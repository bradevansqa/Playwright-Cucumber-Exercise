# Specs

Markdown test plans produced by the `playwright-test-planner` agent land here. The
`playwright-test-generator` agent reads them and writes runnable specs into `tests/`.

This is a separate lane from `features/` - see the root [README](../README.md#playwright-test-agents)
for why the two aren't merged.
